import React from 'react';
import ToolPage from '../ToolPage';

const HexInt: React.FC = () => {
  return (
    <ToolPage
      title="Hex-Integer Converter"
      description="Convert between hexadecimal and decimal integers. Supports signed/unsigned values and different bit widths (8, 16, 32, 64-bit)."
    >
      {/* Tool implementation will go here */}
    </ToolPage>
  );
};

export default HexInt;
