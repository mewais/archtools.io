import React from 'react';
import ToolPage from '../ToolPage';

const HexFloat: React.FC = () => {
  return (
    <ToolPage
      title="Hex-Float Converter"
      description="Convert between hexadecimal and floating-point representations. Supports IEEE 754 formats: FP64, FP32, FP16, BF16, and FP8 variants (E4M3, E5M2)."
    >
      {/* Tool implementation will go here */}
    </ToolPage>
  );
};

export default HexFloat;
