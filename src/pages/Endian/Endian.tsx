import React from 'react';
import ToolPage from '../ToolPage';

const Endian: React.FC = () => {
  return (
    <ToolPage
      title="Endian Swapper"
      description="Swap byte order between big-endian and little-endian representations. Visualize how bytes are arranged in memory."
    >
      {/* Tool implementation will go here */}
    </ToolPage>
  );
};

export default Endian;
